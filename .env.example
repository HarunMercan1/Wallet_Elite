# Supabase Configuration
# Copy this file to .env and fill in your values

# Supabase URL (get from Supabase Dashboard > Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (get from Supabase Dashboard > Settings > API)
# This key is safe to use in client-side code with RLS enabled
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER commit this!)
# Only use for server-side scripts, NEVER in client code
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
